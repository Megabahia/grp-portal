import { AbstractControl, ValidationErrors, ValidatorFn } from "@angular/forms";

export class Rol {
    id: string;
    codigo: string;
    nombre: string;
    descripcion: string;
    config: string;
}
